# 약도 뷰어 간소화(딤드 영역 닫기) 및 갤러리 스와이프 브레인스토밍 (BRAINSTORMING)

## 1. 요청 사항 분석
1. **약도 모달 하단 줌 컨트롤 버튼 제거**:
   - 하단의 `[-]`, `[100%]`, `[+]` 및 안내 문구 제거하여 화면을 더욱 미니멀하고 깔끔하게 정돈.
2. **약도 선택 시 딤드(Dimmed) 배경 및 딤드 영역 클릭 시 복귀**:
   - 약도를 클릭했을 때 뒤 배경이 어둡게 딤드(Dimmed backdrop) 처리됨.
   - 약도 이미지 바깥의 딤드된 배경 영역을 터치/클릭하면 즉시 원래 청첩장 본문으로 닫히고 복귀.
   - 약도 자체에 대한 핀치 줌 및 더블 탭 확대 기능은 깔끔하게 유지.
3. **갤러리 스와이프(Swipe) 지원**:
   - 사진을 클릭하여 모달을 열었을 때, 모바일에서 좌/우로 손가락을 쓸어 넘기면(Swipe) 자연스럽게 다음/이전 사진으로 전환되도록 터치 제스처 탑재.
   - 직관적인 모바일 포토 뷰어 경험 제공.

---

## 2. 세부 설계 방안

### A. 약도 모달 간소화 (`src/components/MapZoomModal.jsx`)
- 하단 플로팅 컨트롤 바 (`[−]`, `[100%]`, `[+]`) 완전히 제거.
- 상단 툴바도 필요 이상으로 답답하지 않도록 깔끔하게 우측 상단 닫기(`×`) 버튼만 미니멀하게 유지.
- **딤드 영역 닫기 메커니즘**:
  - 배경: `bg-black/75 backdrop-blur-xs`로 세련된 딤드 레이어 적용.
  - 약도 이미지 클릭 시에는 `e.stopPropagation()`으로 모달 닫힘 방지(더블탭/핀치 제스처 정상 동작).
  - 약도 이미지 바깥의 모든 딤드 영역 클릭 시 즉시 `onClose()`가 호출되어 원래 화면으로 복귀.

### B. 갤러리 스와이프 기능 (`src/components/Gallery.jsx`)
- 터치 이벤트(`onTouchStart`, `onTouchMove`, `onTouchEnd`)를 활용하여 터치 시작점과 끝점의 가로 이동 거리(`deltaX`) 계산.
- 가로 이동 거리가 45px 이상일 때:
  - 왼쪽으로 스와이프 (`deltaX < -45`) ➔ `showNext()` (다음 사진)
  - 오른쪽으로 스와이프 (`deltaX > 45`) ➔ `showPrev()` (이전 사진)
- 스와이프 중 세로 스크롤과의 간섭을 방지하고 부드러운 스와이프 애니메이션 및 모바일 친화적 UX 제공.
- 갤러리 모달에서도 사진 바깥 딤드 영역 터치 시 즉시 닫히도록 완벽 보장.
